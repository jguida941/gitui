# utils_settings Notes

Purpose
- Provide a tiny in-memory settings store until QSettings is wired.

Flowchart: SettingsStore

[set key/value]
        |
        v
[store in dict]
        |
        v
[get key/value]
