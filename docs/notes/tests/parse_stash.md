# tests_parse_stash Notes

Purpose
- Validate stash parsing with multiple records.

Flowchart: test_parse_stash_records_basic

[build payload with 2 stash records]
        |
        v
[parse_stash_records]
        |
        v
[assert stash fields]
