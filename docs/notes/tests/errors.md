# tests_errors Notes

Purpose
- Verify error types store context and format messages for UI display.

Flowchart: test_command_failed_stores_attributes

[build CommandFailed]
        |
        v
[assert fields + string output]
