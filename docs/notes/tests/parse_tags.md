# tests_parse_tags Notes

Purpose
- Validate tag parsing and empty line handling.

Flowchart: test_parse_tags_basic

[build payload with tags + blank line]
        |
        v
[parse_tags]
        |
        v
[assert tag names]
