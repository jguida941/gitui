#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
VENV_PY="$ROOT/.venv/bin/python"

if [ ! -x "$VENV_PY" ]; then
  echo "GitUI is not installed. Run scripts/install_mac.sh first." >&2
  exit 1
fi

exec "$VENV_PY" -m app.main "$@"
